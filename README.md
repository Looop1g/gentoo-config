# looopig's GentooLinux config

```bash
doas rm -rf {package.accept_keywords, package.mask, package.use, make.conf}
doas ln -snf {package.accept_keywords, package.mask, package.use, make.conf, set}
```
